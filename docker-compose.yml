services:
  crow:
    build: ./crow
    container_name: crow
    ports:
      - "5000:5000"

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./config:/config
      - ./logs:/logs